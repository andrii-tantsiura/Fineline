import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 125,
    borderRadius: 8,
    overflow: "hidden",
    padding: 8,
    alignItems: "flex-end",
    marginBottom: 16,
  },
});

export default styles;
