import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderRadius: 8,
    overflow: "hidden",
    padding: 8,
    marginRight: 18,
    alignItems: "flex-end",
  },
});

export default styles;
