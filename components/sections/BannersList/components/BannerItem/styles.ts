import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 125,
    borderRadius: 8,
    marginBottom: 16,
  },
  image: {
    resizeMode: "contain",
    height: "100%",
    width: "100%",
  },
});

export default styles;
